const classRep1 = 'Alfa Mmosi';
const classRep2 = 'Mercy Chepkemoi';
const classTitle = 'Invictus';
const classClown = 'Eugene Wekesa';

// define age variables for the class reps

const classRep1Age = 39;
const classRep2Age = 18;

const isClassRep1Rich = true;
const isClassRep2Rich = true;

// comic relief
const isEugeneRich = false;

console.log(classRep1 + ' : ' + classRep1Age);
console.log(classRep2 + ' : ' + classRep2Age);

// log booleans

console.log('is ' + classClown + ' Rich ?' + ' : ' + isEugeneRich);
console.log('is ' + classRep1 + ' Rich ?' + ' : ' + isClassRep1Rich);
console.log('is ' + classRep2 + ' Rich ?' + ' : ' + isClassRep2Rich);

// string interpolation
// double quotes
// single quotes
// backticks

const sentence = `Was ${classClown} Rich in the past? ${isEugeneRich}, but he will be rich in the future, possibly`;

console.log(sentence);

// the rules of creating variables
// camelCase
// snake_case

const _one = '';
const one = 1;
const $one = 1;

const chamgei = 'Hello';
const idimaber = 'mambo vipi';
const wantindata = 'how are you doing?';

const this_is _snake_casing = 'this is snake casing';